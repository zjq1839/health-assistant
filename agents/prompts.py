DIETARY_PROMPT = """从用户输入中提取膳食类型、食物描述和日期，返回JSON格式。

用户输入: {content}

提取规则：
1. 膳食类型判断：
   - 包含"早上"、"早餐"、"早晨" -> "早餐"
   - 包含"中午"、"午餐"、"午饭" -> "午餐" 
   - 包含"晚上"、"晚餐"、"晚饭"、"夜宵" -> "晚餐"
   - 如果没有明确时间，默认为"午餐"

2. 食物描述：提取具体的食物和数量信息

3. 日期提取：
   - 如果包含具体日期如"8月1号"、"8月1日"、"8-1"等，提取为YYYY-MM-DD格式
   - 如果是"今天"、"昨天"、"前天"等相对日期，计算具体日期
   - 如果没有日期信息，使用今天的日期
   - 当前年份是2025年

返回格式：{{"meal_type": "膳食类型", "description": "食物描述", "date": "YYYY-MM-DD"}}

示例：
- "8月1号早上喝了一杯牛奶" -> {{"meal_type": "早餐", "description": "一杯牛奶", "date": "2025-08-01"}}
- "昨天晚上吃了200g牛排" -> {{"meal_type": "晚餐", "description": "200g牛排", "date": "2025-08-02"}}

请只返回JSON："""

EXERCISE_PROMPT = """从用户输入中提取运动类型、持续时间（分钟）和描述，返回JSON格式。

用户输入: {content}

提取规则：
1. 运动类型判断：如跑步、游泳、瑜伽等，如果没有指定，默认为"其他"
2. 持续时间：提取数字，如果没有，默认为0
3. 描述：提取额外信息

返回格式：{{"exercise_type": "运动类型", "duration": 数字, "description": "描述"}}

示例：
- "跑了30分钟" -> {{"exercise_type": "跑步", "duration": 30, "description": ""}}
- "游泳1小时，很累" -> {{"exercise_type": "游泳", "duration": 60, "description": "很累"}}

请只返回JSON："""

OCR_EXERCISE_PROMPT = """从以下OCR提取的文本中提取运动类型、持续时间（分钟）和描述，返回JSON格式。

文本: {extracted_text}

返回格式：{{'exercise_type': '运动类型', 'duration': 数字, 'description': '描述'}}

请只返回JSON："""